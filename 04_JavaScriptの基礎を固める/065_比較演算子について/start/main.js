const num = null;
const bool = Boolean(num);
console.log(bool);


if(!num) {
  console.log('this is true');
} else {
  console.log('this is false');
}

// == 型を変更してから
// === 型も一緒
// || or条件
// && and条件
// not演算子
